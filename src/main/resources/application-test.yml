spring:
  datasource:
    url: jdbc:h2:mem:travel_data;DB_CLOSE_DELAY=-1;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  batch:
    jdbc:
      initialize-schema: always
  h2:
    console:
      enabled: true


file:
  hotel: src/main/resources/testData/hotel_bookings.json